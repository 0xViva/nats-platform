# Basic server settings
port: 4222
http: 8222

# Logging
debug: true
trace: false

# JetStream enabled
jetstream {
  store_dir: "/data/jetstream"
  max_mem_store: 1GB
  max_file_store: 10GB
}

# Optional: cluster (disabled by default)
# cluster {
#   name: "nats-cluster"
#   port: 6222
#   routes = []
# }
